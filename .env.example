# UI configuration
#
# The Vite UI works without required environment variables.
# Public settings must be prefixed with VITE_ to be exposed to the browser.

# Optional: the local demo agent URL documented in docker-compose.yml.
VITE_DEMO_AGENT_URL=http://localhost:3001
