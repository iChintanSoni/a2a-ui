# UI configuration
#
# The Next.js UI currently works without required environment variables.
# Keep this file as the place for future public UI settings.

# Optional: the local demo agent URL documented in docker-compose.yml.
NEXT_PUBLIC_DEMO_AGENT_URL=http://localhost:3001
